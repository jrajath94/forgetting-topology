# Core — versions validated on pod ee71k0aupgwzus, 2026-04-16 pilot
torch>=2.4.1,<2.6
transformers>=5.5.0       # Qwen3 support starts here
peft>=0.19.0
trl>=1.1.0                 # SFTTrainer with prompt+completion schema
datasets>=4.0.0           # we use parquet-only sources so the 4.x loader-script ban is fine
accelerate>=1.1.1
# bitsandbytes optional (quantised loading) — add back if we need it

# Eval
evaluate==0.4.3
rouge-score==0.1.2
seqeval==1.2.2
human-eval==1.0.3

# Tracing / interpretability
einops==0.8.0
nnsight==0.2.21          # for activation / weight patching at scale
jaxtyping==0.2.34

# Logging & utils
wandb==0.18.7
tqdm==4.66.5
pyyaml==6.0.2
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
matplotlib==3.9.2
seaborn==0.13.2

# Quality of life
ipython==8.29.0
pytest==8.3.3
